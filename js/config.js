// Point this at your backend's public URL.
// If you're running the backend through a Cloudflare Tunnel, this is the
// tunnel's hostname, e.g. "https://silicon-dos-trains-flying.trycloudflare.com".
// For local testing it's usually "http://localhost:3000".
const API_BASE = "https://pei-cards-api.example.com";
